<Window x:Class="SimpleRadio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Радио" Height="300" Width="450"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15,15,15,0" Height="254" VerticalAlignment="Top">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Интернет Радио" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <GroupBox Grid.Row="1" Header="URL радиостанции" Margin="0,5">
            <TextBox x:Name="UrlTextBox" 
                     Height="30" Margin="5"
                     Text="Введите ссылку"
                     FontSize="12"/>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,15">
            <Button x:Name="StartButton" 
                    Content="▶ START" 
                    Click="StartButton_Click"
                    Width="100" Height="40" Margin="5"
                    FontSize="14" FontWeight="Bold"
                    Background="LightGreen"/>

            <Button x:Name="StopButton" 
                    Content="⏹ STOP" 
                    Click="StopButton_Click"
                    Width="100" Height="40" Margin="5"
                    FontSize="14" FontWeight="Bold"
                    Background="LightCoral"/>
        </StackPanel>

  
    </Grid>
</Window>